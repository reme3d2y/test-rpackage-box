import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { ComponentHeader } from 'storybook/blocks/component-header';

import { Loader } from './Component';
import { name, version } from '../package.json';

<Meta title='Компоненты' component={Loader} />


<!-- Canvas -->

<Story name='Loader'>
    {React.createElement(() => {
        return (
            <Loader />
        );
    })}
</Story>


<!-- Docs -->

<ComponentHeader
    name='Loader'
    version={version}
    package='test-rpackage-box-loader'
    stage={2}
/>

```tsx
import { Loader } from 'test-rpackage-box-loader';
```

Компонент Loader.

<Loader />


<Props of={Loader} />
